spring:
  datasource:
    url: jdbc:mysql://localhost:3306/car_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/maintenance_db

liquibase:
  change-log: classpath:/db/changelog/db.changelog-master.xml

jwt:
  secret: yourSecretKey
  expiration: 86400000


engine-types:
  types:
    - бензиновий
    - дизельний
    - гібридний
    - електричний
fuel-types:
  бензин:
    - А-95
    - А-98
    - А-100
  дизель:
    - зимовий
    - літній
    - арктичний
  електрика: []